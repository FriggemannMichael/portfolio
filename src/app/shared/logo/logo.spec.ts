import { Logo } from './logo';

describe('Logo', () => {
  let component: Logo;

  beforeEach(() => {
    component = new Logo();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
